package ru.stepanov.selfcontrol.api.contract;

/**
 * Машиночитаемые коды ошибок (SCREAMING_SNAKE_CASE) из REST_КОНТРАКТ.yaml.
 */
public enum ErrorCode {
    NOT_FOUND,
    CONFLICT,
    VALIDATION_ERROR,
    UNAUTHORIZED,
    FORBIDDEN,
    INTERNAL_ERROR
}
